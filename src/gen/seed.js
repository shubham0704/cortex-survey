// Deterministic path -> seed hashing, so a given descent path always regenerates
// the same forest/synapse (stable on re-descent). Status: planned.
export function seedFromPath(/* pathIds */){ /* TODO */ return 0; }
